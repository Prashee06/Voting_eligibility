apiVersion: v1
kind: Service
metadata:
  name: voting-eligibility-service
spec:
  selector:
    app: voting-eligibility
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
  type: NodePort